# 技术路线学习规划

## 一、简介

**目标定位：**
 熟悉后端编程与系统设计，对人工智能与数据科学方向具备系统性认知。
 通过持续学习与实践，能够 **独立搭建 AI 驱动的应用系统**，掌握从：

- 数据采集与清洗
- 模型训练与评估
- 服务化接口部署

到最终落地的 **完整工程流程**，逐步形成可复用、可扩展的工程能力。

------

## 第一章：代码管理（Git & GitHub）

### 1.1 创建代码仓库

用谷歌账号注册github

**学习目标：**

- 理解代码仓库的基本概念
- 掌握 GitHub 仓库的创建与初始化

**学习资源：**

- [『教程』一看就懂！Github基础教程（B站）](https://www.bilibili.com/video/BV1hS4y1S7wL)

------

### 1.2 Git 命令操作

**学习目标：**

- 掌握 Git 的常用命令
- 理解 Git 与 GitHub / GitLab 的协作流程
- 能够进行多人协作开发

**学习资源：**

- [人人都能看懂的 Git 教程（B站）](https://www.bilibili.com/video/BV1d6XVYqEuy)

------

#### 1.2.1 初始配置

```
git init
git clone <repo_url>

git config --global user.name "your name"
git config --global user.email "your email"
```

------

#### 1.2.2 基本操作

```
git config --list
git status
git add .
git commit -m "commit message"

git log
git log -p <file>
git log --oneline
git log --graph
```

------

#### 1.2.3 分支操作

```
git branch <branch_name>
git checkout <branch_name>
git switch <branch_name>

git checkout -b <branch_name>
git switch -c <branch_name>

git branch -d <branch_name>
git branch -D <branch_name>

git merge <branch_name>
git rebase <branch_name>
```

------

#### 1.2.4 远程仓库操作

```
git remote add origin <repo_url>
git remote -v

git push -u origin <branch>
git pull origin <branch>
git fetch origin
```

------

#### 1.2.5 储藏（stash）操作

```
git stash
git stash list
git stash pop

git stash apply stash@{n}
git stash drop stash@{n}
git stash clear

git stash push -m "message" -u
```

------

#### 1.2.6 撤销与回退

```
git reset --soft HEAD~1
git reset --hard HEAD~1

git reset <file>
git checkout -- <file>
```

------

#### 1.2.7 差异比较

```
git diff
git diff --cached
git diff HEAD
```

------

#### 1.2.8 Rebase 操作

```
git rebase --continue
git rebase --skip
git rebase --abort
```

------

## 第二章：MySQL 数据库基础

### 2.1 MySQL 学习资源

- [2026 最新版 MySQL 基础教程（B站）](https://www.bilibili.com/video/BV17pfRBAEqu)
- navicat 软件

------

### 2.2 SQL 操作练习（50 题）

#### 2.2.0 表结构定义

##### 用户表（users）

```
DROP TABLE IF EXISTS users;
CREATE TABLE users (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
    name VARCHAR(50) NOT NULL COMMENT '用户姓名',
    phone VARCHAR(20) UNIQUE COMMENT '手机号',
    age INT COMMENT '年龄',
    gender TINYINT COMMENT '性别：1男 2女',
    status TINYINT DEFAULT 1 COMMENT '状态：1正常 0禁用',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) COMMENT='用户表';
```

##### 订单表（orders）

```
DROP TABLE IF EXISTS orders;
CREATE TABLE orders (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY COMMENT '订单ID',
    order_no VARCHAR(32) NOT NULL UNIQUE COMMENT '订单号',
    user_id BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    amount DECIMAL(10,2) NOT NULL COMMENT '订单金额',
    status TINYINT COMMENT '订单状态：0待支付 1已支付 2已取消',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    paid_at TIMESTAMP NULL COMMENT '支付时间',
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) COMMENT='订单表';
```

------

#### 2.2.1 示例数据

```
INSERT INTO users (name, phone, age, gender, status, created_at) VALUES
('张三', '13800000001', 28, 1, 1, '2024-01-01 10:00:00'),
('李四', '13800000002', 35, 1, 1, '2024-01-05 11:00:00'),
('王五', '13800000003', 22, 2, 1, '2024-01-10 09:30:00'),
('赵六', '13800000004', 30, 2, 1, '2024-01-15 14:20:00'),
('孙七', '13800000005', 40, 1, 0, '2024-01-20 16:45:00');
INSERT INTO orders (order_no, user_id, amount, status, created_at, paid_at) VALUES
('ORD202401010001', 1, 199.00, 1, '2024-02-01 10:10:00', '2024-02-01 10:15:00'),
('ORD202401010002', 1, 599.00, 1, '2024-02-05 11:00:00', '2024-02-05 11:05:00'),
('ORD202401010003', 2, 1299.00, 1, '2024-02-07 09:30:00', '2024-02-07 09:40:00'),
('ORD202401010004', 2, 399.00, 2, '2024-02-08 13:20:00', NULL),
('ORD202401010005', 3, 89.00, 1, '2024-02-10 15:00:00', '2024-02-10 15:05:00'),
('ORD202401010006', 3, 999.00, 0, '2024-02-12 18:30:00', NULL),
('ORD202401010007', 4, 2599.00, 1, '2024-02-15 20:00:00', '2024-02-15 20:10:00');
```

### 2.3 SQL 练习题目录（共 50 题）

1. 查询 `user` 表中所有用户信息
2. 查询 `user` 表中年龄大于 18 岁的用户
3. 查询 `user` 表中所有用户的姓名和手机号
4. 查询 `user` 表中性别为“男”的用户数量
5. 查询 `user` 表中按注册时间倒序排列的前 10 个用户
6. 查询 `user` 表中手机号不为空的用户
7. 查询 `user` 表中年龄在 20～30 岁之间的用户
8. 查询 `user` 表中状态为“正常”的用户
9. 查询 `user` 表中姓名以“张”开头的用户
10. 查询 `user` 表中注册时间最早的用户
11. 查询 `order` 表中状态为已支付且金额大于 1000 的订单
12. 查询 `order` 表中创建时间在最近 7 天内的订单
13. 查询 `order` 表中订单金额最高的 1 条记录
14. 查询 `order` 表中金额在 500～2000 之间的订单
15. 查询 `order` 表中状态不为已取消的订单
16. 查询 `order` 表中按金额升序、创建时间降序排序
17. 查询 `order` 表中状态为已支付的订单数量
18. 查询 `order` 表中支付时间不为空的订单
19. 查询 `order` 表中金额小于 100 的订单
20. 查询 `order` 表中创建时间最早的 5 条订单
21. 查询 `order` 表中订单总数
22. 查询 `order` 表中订单总金额
23. 查询 `order` 表中平均订单金额
24. 查询 `order` 表中每个用户的订单数量
25. 查询 `order` 表中每个用户的订单总金额
26. 查询 `order` 表中订单数量大于 5 的用户
27. 查询 `order` 表中每天的订单金额汇总
28. 查询 `order` 表中每种订单状态的订单数量
29. 查询 `order` 表中最大订单金额和最小订单金额
30. 查询订单总金额最高的用户
31. 查询每个订单对应的用户名和订单金额
32. 查询下过订单的用户信息（去重）
33. 查询没有下过订单的用户
34. 查询每个用户的订单数量（包含 0 单用户）
35. 查询订单金额最高的用户
36. 查询每个用户的最近一笔订单信息
37. 查询每个用户已支付订单的数量
38. 查询每个用户已支付订单的总金额
39. 查询至少下过 2 笔订单的用户
40. 查询订单金额大于 1000 的订单及其用户信息
41. 查询订单金额大于平均订单金额的订单
42. 使用 EXISTS 查询下过订单的用户
43. 查询订单数量最多的用户
44. 查询订单金额最高的那一笔订单对应的用户
45. 查询没有已支付订单的用户
46. 查询订单数量大于平均订单数量的用户
47. 查询每个用户金额最高的一笔订单
48. 查询至少有一笔订单金额大于 2000 的用户
49. 查询订单总金额排名前 3 的用户
50. 查询最近一笔订单金额高于该用户平均订单金额的用户

## 第三章：python

### 3.1、学习资源

​	[【全748集】目前B站最全最细的Python零基础全套教程，2024最新版，包含所有干货！七天就能从小白到大神！少走99%的弯路！存下吧！很难找全的！_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1rpWjevEip/?spm_id_from=333.337.search-card.all.click&vd_source=d9cf01c9d1b98c154926b876f7416d41)

### 3.2、**学习路线**

- 基础学习（60天）

  1. 命令行工具
  2. 小型脚本

- 各种库学习（30天）

  1. 工具型项目比如基金实时评估工具

- 接口开发（30天）

  1. 完整 CRUD 系统
  2. 用户 / 权限 / 订单模块 等

- 数据爬虫（30天）

  1. 数据采集系统
  2. 定时爬虫任务（收集新闻，收集股票数据、收集微博信息等）

- 大数据开发（90天）

  1. 数据分析平台
  2. 日志 / 行为分析系统

- 深度与机械学习（180天）

  1. 日志 / 行为分析系统 
  2. 预测模型 

- 大模型开发（3-6个月）

  1. AI 助手
  2. 智能问答系统
  3. 企业知识库

  ## 每天至少学习2-3小时！！！













